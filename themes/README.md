# Theme files

Put real or private Power BI theme JSON files in `themes/local/`. That folder
is ignored by Git, so its contents will not be uploaded to GitHub.

Only add sanitized, shareable themes to `themes/examples/`. The development
fixture used by the app currently lives at
`public/fixtures/private-starter-theme.json`.
